{
  "name": "@bonsai/core",
  "version": "0.1.0",
  "description": "Framework-agnostic Bonsai domain: tree/branch/merge/distill primitives, ContextPacket assembly, pluggable Storage/LLMProvider/WikiStore/Retriever interfaces.",
  "private": true,
  "publishConfig": {
    "access": "restricted"
  },
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b",
    "test": "vitest run",
    "test:watch": "vitest",
    "publint": "publint --strict",
    "attw": "attw --pack . --profile esm-only",
    "prepublishOnly": "node -e \"console.error('Publishing @bonsai/* is disabled (BONA-247: packages are private).'); process.exit(1)\""
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "vitest": "^2.1.0"
  }
}
